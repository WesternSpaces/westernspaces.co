---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span
  class:list={[
    'inline-block text-xs font-semibold uppercase tracking-wider',
    'px-3 py-1 rounded-full bg-parchment text-ridge',
    className,
  ]}
>
  <slot />
</span>
